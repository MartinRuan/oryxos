# Specification Quality Checklist: 第22节 Memory 实现与代码讲解

**Purpose**: 校验 `specs/022-lesson22-memory/spec.md` 需求规约的完备性、准确性与可执行性。

## 1. 业务与价值对齐
- [x] 一句话定位明确：统一三层记忆门面，会话记忆走 SQLite、长期记忆走两分区 `MEMORY.md`，内置 Agent 读写工具。
- [x] 核心价值明确：提供跨会话偏好持久化能力，为 Demo 二（每日科技日报）打通基石。
- [x] 明确不做边界清晰：不做自动提炼、不做向量库/Embedding、不做情景记忆、不做 Memory Wiki、不做递归压缩。

## 2. 功能需求与验收标准
- [x] 需求具备可测试性：每个功能点（FR-001 ~ FR-007）对应可运行的单测。
- [x] 关键回归点原样对齐课件：
  - `截断只裁归档区_核心记忆一字不能少`
  - `写入后立刻可读_不允许有缓存`
  - `scope路由到正确区块_缺省写入归档区`
  - `recallByKeyword只搜归档区_不区分大小写`
  - `scope缺省写归档`
  - `关键词未命中返回友好提示不报错`
  - `buildContext返回核心记忆与会话历史的组合_归档区不整体注入`
- [x] 人工验收项与自动化验收项明确拆分。

## 3. 边界与防御
- [x] 文件不存在自动初始化带有两分区的默认文件。
- [x] 非法 `scope` 安全回退至 `ARCHIVAL`。
- [x] 检索空结果友好提示，防异常穿透。
